~[tlist_sql;
SELECT 
    count(U_CDOL_STAFF_CHANGES.id)
FROM U_CDOL_STAFF_CHANGES
WHERE 
    (
        (U_CDOL_STAFF_CHANGES.ps_created !=1 OR U_CDOL_STAFF_CHANGES.ps_created IS NULL)
        OR 
        (U_CDOL_STAFF_CHANGES.ad_created !=1 OR U_CDOL_STAFF_CHANGES.ad_created IS NULL)
        OR 
        (U_CDOL_STAFF_CHANGES.lms_created !=1 OR U_CDOL_STAFF_CHANGES.lms_created IS NULL)
    )
AND 
    U_CDOL_STAFF_CHANGES.yearid= (EXTRACT(YEAR FROM CURRENT_DATE)-1991)
    ~[if.is.a.school] AND U_CDOL_STAFF_CHANGES.schoolid=~(curschoolid) [/if]
;]~(cnt)[/tlist_sql]